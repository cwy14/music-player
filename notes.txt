display:none    隐藏且不占空间
visibility:hidden    隐藏但占据原来位置

----------------------------------------------------------------------------------

拖动元素过快会导致选中内容,影响拖动,解决方法,在onmousemove句柄中增加:
window.getSelection?window.getSelection().removeAllRanges():document.selection.empty();

-------------------------------------------------------------------------------------

array.indexOf()返回匹配元素在数组中首次出现的位置下标,没有则返回-1,
不同于string.indexOf(),不会做隐式转换,而是使用"==="比较 

------------------------------------------------------------------------------------

Math.random()返回值范围:[0,1)